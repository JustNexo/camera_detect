-- Состояние камер на сайте хранится в camera_presence (heartbeat/stream_frame).
-- Старые строки НЕ удаляются при смене имён камер на объекте — они остаются «офлайн»
-- и увеличивают «Требуют внимания», хотя этих камер уже нет в вашей SQLite на клиенте.
--
-- Вариант 1 — сбросить только один объект (подставьте точное имя из SITE_NAME / site в JSON):
-- DELETE FROM camera_presence WHERE scope_key LIKE '%"site": "ФФО-2"%';
--
-- Вариант 2 — полная очистка присутствия (после деплоя клиенты снова заполнят таблицу):
-- DELETE FROM camera_presence;
--
-- Вариант 3 — на сервере в .env: CAMERA_PRESENCE_PRUNE_DAYS=30
-- (при каждом старте приложения удаляются строки с last_seen старше N дней).
